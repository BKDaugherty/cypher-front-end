import React from 'react';
import {StyleSheet, SafeAreaView, Text, View, TouchableHighlight } from 'react-native';
import AppLogo from '@Components/AppLogo.js'

export default (props) => (<SafeAreaView><AppLogo/></SafeAreaView>)